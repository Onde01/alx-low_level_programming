#iclude <stdio.h>

/**
 * main - Print the alphabet.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char alp(26) = "abcdefghijklmnopqrstuvwxyz;
	int i;
	for (i = 0; 1 < 26, i++)
	{
		puchar(alp[1]);
	}
	putchar(" /n");
	return (0);
}

